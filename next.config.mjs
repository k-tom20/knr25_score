/** @type {import('next').NextConfig} */
const nextConfig = {
  /* config options here */
  output: "export",
  basePath: '/knr25_score',
  assetPrefix: '/knr25_score/',
};

export default nextConfig;
